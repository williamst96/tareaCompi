### Descargar el archivo flex.l
### En la terminal, ubicado en la carpeta donde se encuentre el archivo "flex.l" ejecutar los siguientes comandos:
```
>  $ flex flex.l
>  $ gcc lex.yy.c -lfl
>  $ ./a.out <ArchivoDeTextoPorProbar>
```

